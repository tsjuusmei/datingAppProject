<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('head') %>
</head>
<body>
    
    <title><%= data.title %> - My movie website</title>
    <h1><%= data.title %></h1>
    <% if (data.cover) { %>
    <img alt
      src="/upload/<%= data.cover %>"
    >
    <% } %>
    <p><%= data.description %></p>

    <button
    id=js-remove
    data-id="<%= data.id %>"
    class=danger
    >Remove</button>

    <%- include('tail') %>

</body>
</html>